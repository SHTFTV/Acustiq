{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run generate:sitemap && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/((?!assets/|robots.txt|sitemap.xml).*)", "destination": "/index.html" }
  ]
}
